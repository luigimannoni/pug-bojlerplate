mixin container(classes)
  - var attributes = {class: `container ${classes}`}
  table(width="660", align="center", border="0", cellpadding="0", cellspacing="0")&attributes(attributes)
    tbody
      tr
        if block
          block